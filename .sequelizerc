const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'shared', 'infra', 'database', 'sequelize.ts'),
  'models-path': resolve(__dirname, 'src', 'modules', 'song', 'infra', 'entities'),
  'migrations-path': resolve(__dirname, 'src', 'shared', 'infra', 'database', 'migrations'),
  'seeders-path': resolve(__dirname, 'src', 'shared', 'infra', 'database','seeds'),
}
